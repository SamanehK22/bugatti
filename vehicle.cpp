#include "vehicle.h"

Vehicle::Vehicle(string company, string model, string color, int year, int maxSpeed):company(company), model(model), color(color), publishDate(year), maxSpeed(maxSpeed){}

Vehicle::~Vehicle(){}

